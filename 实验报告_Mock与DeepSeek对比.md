# Mock 规则路径与 DeepSeek LLM 路径对比实验报告

**项目**：SURF 多智能体校园舆情仿真（ABCDE 模块联调）  
**实验目录**：`联调_ABCDE`  
**数据**：`output_mock_100x50` / `output_ds_100x20`  
**随机种子**：42  
**报告日期**：2026-07-26  

---

## 1. 实验目的

在已完成的 ABCDE 模块联调框架上，验证全链路仿真在两类决策后端下的可运行性与指标行为差异：

1. **离线 Mock / 规则降级路径**：用于快速压力测试与接口验收；  
2. **真实 DeepSeek API 路径**：用于检验 LLM 驱动的 BDI 信念—情绪更新是否能够产生可观测的舆情演化信号。

同时记录运行耗时，为后续更大规模实验提供基准。

---

## 2. 实验思路

校园舆情场景设定为四类群载体（宿舍 / 班级 / 专业 / 校园）与四类角色（普通成员、活跃讨论者、理性讨论者、管理者）。仿真分工为：

| 模块 | 职责 |
|------|------|
| A | 环境调度、热度演化、DataCollector |
| B | 智能体 BDI 循环 |
| C | Hawkes 激活与 LLM 工具链 |
| D / E | 离线评估与结果导出 |

本实验**固定**网络结构、角色比例与随机种子，**仅切换 LLM 后端**，从而把「骨架是否跑通」与「大模型是否改变内生状态」分开观察。

- Mock 实验运行 **50 步**，观察长时间稳定性；  
- DeepSeek 实验运行 **20 步**，控制 API 时延与费用；  
- 结果分析以重叠时间窗 **tick 0–19** 为主，保证两条曲线可逐点对照。

---

## 3. 实验设置

| 项目 | Mock 实验 | DeepSeek 实验 |
|------|-----------|---------------|
| 配置文件 | `config_100_mock.yaml` | `config_100_ds.yaml` |
| 智能体数量 | 100 | 100 |
| 仿真步数 | 50 | 20 |
| 网络 | Barabási–Albert（m=3） | 同左 |
| 角色比例 | ORDINARY 70% / ACTIVE 10% / RATIONAL 10% / CONTROLLER 10% | 同左 |
| 群类型比例 | DORM 25% / CLASS 35% / MAJOR 25% / CAMPUS 15% | 同左 |
| LLM 后端 | MockLLMClient（空更新） | deepseek-v4-flash |
| thinking | — | disabled |
| 随机种子 | 42 | 42 |
| 结果目录 | `output_mock_100x50` | `output_ds_100x20` |

### 观测指标

| 指标 | 含义 |
|------|------|
| `avg_opinion` | 全网平均观点 |
| `polarization` | 观点标准差（极化程度） |
| `emotional_contagion` | 相邻步情绪变化强度 |
| `message_count` | 近窗消息存量（非累计发帖） |
| `negative_emotion` | valence &lt; 0 的智能体比例 |
| `distortion_level` | 近窗消息失真均值 |
| `cross_group_forward` | 跨群扩散累计计数 |
| `intervention_tick` | 最早干预时刻 |
| `recovery_time` | 热度回落恢复时长 |
| `step_seconds` | 每步墙钟耗时 |

---

## 4. 实验内容与流程

1. 组装联调环境，统一 Mesa 3.0.3 与模块接口；  
2. 执行 Mock 压力仿真，导出 `metrics.csv` / `run_meta.json`；  
3. 在相同网络与种子下执行 DeepSeek 仿真；  
4. 对重叠 20 步窗口进行指标对比，并结合源码核对指标语义（尤其是跨群计数与干预时刻的实现定义）。

---

## 5. 主要结果

### 5.1 运行性能

| 项目 | Mock | DeepSeek |
|------|------|----------|
| 总墙钟时间 | **0.84 s** | **1685 s（约 28.1 min）** |
| 平均步耗时 | 0.017 s | 84.2 s |
| 步耗时范围 | 0.005–0.019 s | 33.6–95.5 s |
| 数据完整性 | 50 行，NaN = 0 | 20 行，NaN = 0 |

Mock 适合接口回归与长步数压力；DeepSeek 量级与「每步大量同步 API 调用」一致，20 步数据完整，说明真模型路径可用，但规模扩展需继续控制步数或并发策略。

### 5.2 重叠窗口末态（tick = 19）

| 指标 | Mock | DeepSeek | 说明 |
|------|------|----------|------|
| `avg_opinion` | 0.043 | −0.053 | Mock 全程不变；DS 略偏负 |
| `polarization` | 0.601 | 0.962 | DS 极化显著升高 |
| `negative_emotion` | 0.58 | 0.87 | DS 负面比例更高 |
| `message_count` | 151 | 111 | 近窗存量，非累计发帖 |
| `distortion_level` | 0.0025 | 0.0725 | DS 近窗失真略高 |
| `cross_group_forward` | **29** | **29** | 前 20 步轨迹完全一致 |
| `intervention_tick` | 0.0 | 0.0 | 实现语义需谨慎解读 |
| `recovery_time` | 0.0 | 0.0 | 表示尚未记录回落时长 |

补充：Mock 在 tick = 49 时 `cross_group_forward` 累计至 **94**；观点与极化在全部 50 步内保持初始值不变。

---

## 6. 演化特征摘要（tick 0–19）

1. **平均观点**：Mock 钉死在 0.043；DeepSeek 由 +0.024 缓慢漂移至约 −0.053。  
2. **极化**：Mock 钉死在 0.601；DeepSeek 由约 0.64 升至约 0.96。  
3. **负面情绪**：Mock 由 0.49 缓升至 0.58；DeepSeek 由 0.49 升至约 0.87。  
4. **近窗消息量**：早期接近，随后均先升后降（缓存过期淘汰），形态相似。  
5. **跨群累计**：两条实验在 tick 0–19 **逐点相同**（2→29）。

对应折线图见联调目录出图结果，或 Cursor 中的交互报告页。

---

## 7. 结果分析

### 7.1 联调骨架已验证

两类实验均完整产出 DataCollector 时序，必需指标列齐全且无缺失。消息近窗、跨群热度累计、逐步耗时等宏观量均可复现。这表明 A（调度/环境）—B（BDI）—C（Hawkes/LLM 接口）—导出链路在 100 智能体规模下可稳定运行。

### 7.2 Mock 与 DeepSeek 的差异来源

- Mock 路径下平均观点与极化在 50 步内保持恒定，说明 MockLLM 对信念更新近似为空操作；负面情绪仅缓慢上升，主要来自规则侧情绪/行动反馈。  
- DeepSeek 路径下观点均值出现小幅负向漂移，极化与负面情绪明显上升，表明真实 LLM 写入的 `opinion_updates` / `emotion_delta` 已作用于智能体内生状态。  
- 两条实验在消息活跃度形态上相近，说明传播层骨架相似，**差异主要体现在认知—情绪层**。

### 7.3 对「跨群传播」指标的说明

在 seed = 42 下，tick 0–19 的 `cross_group_forward` 序列逐点相同。结合实现可知：

- 当前智能体仅归属**单一** `group_type`；  
- 感知也限定为**同群**消息；  
- 跨群累计主要来自环境模块在热度超过阈值后的**群间热度溅射**。

因此该指标适合描述「热度是否发生跨群溢出」，尚不能直接解释为「宿舍消息被某人转发至班级/专业群」的微观转发链。若需支撑多层群聊叙事，后续应扩展多重群隶属或显式跨群转发动作。

### 7.4 干预与恢复指标

两组实验中 `intervention_tick` 与 `recovery_time` 均显示为 0.0。按当前代码语义：

- `recovery_time = 0` 表示尚未记录「热度回落完成」的时长；  
- `intervention_tick = 0.0` 更可能表示极早触发或展示层对缺省值的处理，不宜直接表述为「第 0 步完成有效治理」。

相关治理效果评估需在明确触发日志与对照实验后进一步展开。

---

## 8. 结论

1. ABCDE 联调在 100 智能体规模下可稳定完成压力测试与指标采集；  
2. Mock 路径适用于快速验收与长步数骨架测试；DeepSeek 路径在约 28 分钟 / 20 步的成本下可观测到明确的信念与情绪演化；  
3. 同种子对比表明，大模型主要影响认知—情绪层，而跨群累计指标在当前实现中由环境热度溅射主导；  
4. 多层群聊「小群向大群转发」的微观机制仍不完整，是下一阶段建模重点。

---

## 9. 局限与后续工作

**局限**

- DeepSeek 仅 20 步，与 Mock 50 步时长不完全对称；  
- 跨群与干预指标的场景语义仍弱于叙事设定；  
- LLM 行动类型尚未完全驱动执行链路（当前优先体现为信念/情绪更新）。

**后续工作**

1. 扩展智能体多重群隶属或桥接转发；  
2. 分离「热度溅射次数」与「消息跨群转发次数」指标；  
3. 在固定种子下增加干预策略对照；  
4. 在可控费用下将真模型步数适度延长，并保留消息级日志以便解释极化来源。

---

## 附录：数据文件

| 文件 | 说明 |
|------|------|
| `output_mock_100x50/metrics.csv` | Mock 逐步指标 |
| `output_mock_100x50/run_meta.json` | Mock 运行元信息 |
| `output_ds_100x20/metrics.csv` | DeepSeek 逐步指标 |
| `output_ds_100x20/run_meta.json` | DeepSeek 运行元信息 |

本报告图表与数值均由上述原始时序生成。
